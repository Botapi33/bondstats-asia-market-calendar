import fs from 'node:fs'; import {markets} from './catalog.mjs'; import {normalizeEvent} from './normalize.mjs'; import {validate} from './validate.mjs'; import {officialSeed} from './sources/official-seed.mjs';
const events=officialSeed().map(normalizeEvent).sort((a,b)=>(a.date+(a.time||'')).localeCompare(b.date+(b.time||'')));
const payload={meta:{product:'BondStats Asia Market Calendar',version:'1.0.0',generatedAt:new Date().toISOString(),methodology:'Only dates/times supported by named official institutional sources are published as confirmed. Facts are normalized; source prose, charts, logos and proprietary calendar compilations are not reproduced.',markets:markets.map(({code,name})=>({code,name})),eventCount:events.length},events};
validate(payload); fs.mkdirSync(new URL('../data/',import.meta.url),{recursive:true}); fs.writeFileSync(new URL('../data/events.json',import.meta.url),JSON.stringify(payload,null,2)+'\n'); console.log(`Wrote ${events.length} confirmed events`);
